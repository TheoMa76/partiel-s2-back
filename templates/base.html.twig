<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Gestion Location | Lac de Poses{% endblock %}</title>
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
        <style>
            .background-blur {
                background-image: url('{{ asset('images/background.jpg') }}');
                background-size: cover;
                background-position: center;
                filter: blur(8px);
                position: absolute;
                height: 100%;
                min-height: 90vh;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                z-index: -1;
                opacity: 0.6;
            }
            body {
                position: relative;
                margin: 0;
                padding: 0;
            }
        </style>
    {% endblock %}
</head>
<body>
    <div class="background-blur"></div>
    
    <nav class="bg-gray-800 text-white py-4 fixed w-full top-0 left-0 z-10">
        <div class="container mx-auto px-4 flex items-center justify-between">
            <div class="text-2xl font-bold">
                <a href="{{ path('app_materiel_index') }}" class="hover:text-gray-400">Gestion Location | Lac de Poses</a>
            </div>
            <ul class="flex space-x-6">
                <li>
                    <a href="{{ path('app_materiel_index') }}" class="hover:text-gray-400">Matériel</a>
                </li>
                <li>
                    <a href="{{ path('app_commande_index') }}" class="hover:text-gray-400">Commande</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="relative z-20 mt-20">
        {% block body %}{% endblock %}
    </div>

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</body>
</html>
